# Composants archivés (non utilisés dans l'app)

Les composants suivants ne sont pas importés dans `App.tsx` et sont conservés pour référence ou réutilisation future :

- **LightPillar.tsx** — Fond WebGL (pilier lumineux). Déplacé ici : voir `../LightPillar.tsx` (fichier laissé en place pour éviter les casses d’import).
- **animated-shader-hero.tsx** — Hero avec shader WebGL.
- **animated-shader-background.tsx** — Arrière-plan shader animé.

Pour les réactiver : importer dans `App.tsx` et les monter dans l’arbre de rendu.
